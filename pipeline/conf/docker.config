docker {
  enabled = true
  runOptions = "${params.geniac.containers.dockerRunOptions}"
}

process {
  withLabel:snv { container = "snv" }
  withLabel:sv { container = "sv" }
  withLabel:plots { container = "plots" }
}
