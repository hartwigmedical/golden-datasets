conda {
  cacheDir = "${params.condaCacheDir}"
}

process {

  beforeScript = "export R_LIBS_USER=\"-\"; export PYTHONNOUSERSITE=1"

  withLabel:snv { conda = "${params.geniac.tools.snv}" }
  withLabel:sv { conda = "${params.geniac.tools.sv}" }
  withLabel:plots { conda = "${params.geniac.tools.plots}" }
}
